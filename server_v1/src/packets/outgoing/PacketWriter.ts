/**
 * A wrapper helper class,
 * stops us having to import every single packet...
 * we simply can write: PacketWriter.write81(data).
 * Please note alex, pass the socket into the constructor of this class,
 * it'll make shit much cleaner!
 */
